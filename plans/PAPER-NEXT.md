# Paper — current writing task

**Active. Owner:** Antigravity for scoped writing/building; Codex for delicate
claim changes. Work in the separate repo `67a9ea6275d3d9785ce57026/`, following
its `AGENTS.md` and marker convention. This supersedes the archived P1 dispatch.

## Evidence boundary

BP51/BP52 review: all existing video sources have prior development/training
use; zero independent confirmation matches are accepted. The short native
CRF-only diagnostic has fresh controls and no recorded alarms, but no winning
point. After BP53 notes the PDF is 27 pages (body/references 1–22, appendices
23–27), within the project's 23+5 budget.
BP53 half-scale search also established no win. Its driver-identity crossing
and incomplete historical total timing remain explicit; later client/budget
repairs are not native reruns. BP54 is provisional triage. See BP56/BP57 next.
Do not re-run the completed appendix move. Separate PointStream encode/decode
time from scoring before any final three-way comparison.

Canonical background contexts, explicit conventional fallback and repaired
recovery are implemented. The native preflight is an engineering diagnostic,
not E1 evidence. No confirmed modern same-frame AV1/VVC winning curve exists.
Synthetic interruption tests establish recovery behaviour, not native runtime
limits or a codec win. Preserve historical negative findings with their scope.

No live-operation promise: canonical canvas preparation is offline. Background
reuse does not guarantee a bitrate win. Fallback has routing and analysis costs;
the automatic mixed-scene scheduler is not validated. Raw source passthrough is
not a coded reference. Generation stays off during the initial low-rate search.
YouTube-derived footage is not promised for redistribution. Planned artifacts
are code, configuration, source identifiers and extraction/annotation procedures,
with source material released only where rights permit.

## Next steps and acceptance

1. Maintain section-local STATUS/NOTE/HOLE markers. Never clear a result hole
   with a plan or smoke test. The paper itself is the research record.
2. After BP49/BP45 produce verified results, insert the operating regime, complete
   byte ledger, declared quality axes, timing, uncertainty, search history and
   failures. Each number needs an output path and a passing citability verdict.
3. Freeze the claim before confirmation and core ablations. Add a credible
   learned-video-codec comparison and the second domain in roadmap order.
4. Reduce appendices and rebalance the body before final evaluation tables
   arrive. Preserve technical evidence in versioned companion material rather
   than deleting it to meet the limit. The project budget is 23 pages including
   references plus at most 5 appendix pages; count the rendered pages separately.
5. Write the conclusion once the central result is settled. The manuscript is
   not submission-ready while that result and conclusion are missing.

Build from the paper repo with
`conda run -n tex --no-capture-output bash tools/build_pdf.sh`.
Return the paper commit, command/PDF path, actual body/reference/appendix page
counts, citation/label errors, changed claims with provenance, remaining holes
and page-budget action. Do not install TeX into the pinned code environment.
Evidence freezes on 20 September; submit by 30 September.
