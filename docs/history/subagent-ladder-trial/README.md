# Demo subagent-ladder trial records

Each eligible real demo task creates one `<card-id>.md` record here, following
[the trial protocol](../../workflow/session/subagent-ladder-trial.md). Records
are append-only after allocation so concurrent sessions do not edit one ledger.
