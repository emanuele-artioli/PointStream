# Evaluation campaign — current decision wave

The [17 September submission decision wave](tasks/20260917-submission-decision-wave.md) is the sole current execution plan. It replaces launch directives in the 14–17 September campaign documents. Those documents and their immutable artifacts remain historical evidence; completed repairs #127, #128, #129, and #132 are not assigned again.
The previous campaign is preserved in
[plan-20260914-history.md](plan-20260914-history.md); its same-directory links
remain valid, but its dispatches are superseded.

| Status | Work | Release |
|---|---|---|
| **Current** | Phase 0 diagnostics, preparation, neural-anchor/session readiness | Explicit bounds in the decision-wave card |
| **Conditional** | Compact PointStream inference/encode and development decision screen | Only after named dependencies and cost cards pass |
| **Not released** | Confirmation scores, broad training, broad neural ladder, paper claims | 20 September decision plus frozen procedure |
| **Blocked** | SOTA/neural comparison | Actual neural roundtrip or documented specific blocker |

The screen uses at most two development windows per domain, two rate targets,
and six new PointStream settings per domain within 30 minutes including decode
and scoring. It compares complete PointStream, uniform AV1/VVC, verified
conventional ROI, simple paste/genuine warp, and a published neural anchor.
Presley/charged GenStream/fusion are diagnostics, never duplicate Cartesian
arms. Reuse the existing ROI utilities; verify actual ROI effect, final QP/CRF,
encoder version, standalone frame count, and timing.

All output rows must include source identity/hash grid, container and full byte
ledger, actual settings, decoded pixels/frame hashes, metric calibration,
two-sided bounds, and encode/client time. A failed cap returns a cost card; it
does not silently drop a comparator. No score-free audit reads quality scores.
